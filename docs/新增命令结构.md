# remote 初始指令

当前有一个 git-back-switch.go，里面含有部分后端切换逻辑，集成到该项目

ggt remote --https  切换远程后端服务为 https
ggt remote --ssh    切换远程后端服务为 ssh

使用我的设计，或者探讨更合理的设计。

要求参考 ggt sum，保证新命令的输出也和 pterm 深度融合。

## 其他任务

编写一个构建脚本，让我可以一键构建多个平台的二进制文件。
