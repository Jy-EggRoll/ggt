# 必读项目规范

- 项目处于开发验证期，如果有破坏性更新，需要删原来的就删原来的，不用考虑兼容性和迁移性
- 始终用中文回复我
- 在动手之前，尽可能多地查阅已有代码，找到每一处修改的前因后果，列出所做的每一项更改的潜在影响点
- 尽可能地多查阅 context7 以及多联网搜索，只做有根据的更改
- 每次改完代码后必须执行 `task`（即 Taskfile 的 default 任务）做完整的构建，并使用一切可以利用的手段（Go 单测、curl API 调用、分析 HTML DOM 树等）自我验证，确保改动正确且没有破坏已有功能。所有验证通过后才能交付，杜绝返工
- 项目各个源代码文件必须有详尽的中文注释（注释结尾不需要加句号），讲清楚前因后果、潜在影响点，改代码的时候同时维护注释，千万不要出现注释和代码不一致的情况
- 修复问题时，不能目光短浅地聚焦于当下的问题，尽可能通读所有代码，修正类似的问题，并维护相关注释
