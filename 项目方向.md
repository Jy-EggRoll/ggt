# 项目方向

当前我有一些 pwsh 脚本和 fish 脚本，用于管理我的大量的仓库。但是，因为语法不统一，不跨平台，所以非常不方便。于是我想用 Go 来重写这些脚本，来管理我的仓库。

我希望这个程序足够现代化，引入 cobra、viper 等库来管理命令行参数和配置文件，引入 pterm 或其他库来确保输出美观、炫彩、易于阅读。

更重要的是，我希望这个脚本的性能足够高，因为我又大量的仓库需要管理，所以并发是必须的。此外，太过高的并发也可能会导致瞬间吃满 CPU，毕竟 git 并不算轻量级的操作。根据大部分用户电脑的配置，我建议并发数控制在用户逻辑 CPU 数的一半左右，用户可以通过命令行参数或配置文件来调整这个值。

目前需要实现的命令如下：

## 命令列表

### 基础命令

- `ggt status` - 显示所有仓库的 git 状态（简写为 `ggt st`）
- `ggt size` - 显示所有仓库的大小统计信息（简写为 `ggt sz`）

### 提交命令

- `ggt summary` - 遍历所有仓库，显示变更，询问一键提交（简写为 `ggt sum`）
  - 支持 `git add -A` 全部提交
  - 自动生成提交信息（格式：`🔨 chore: 终端推送更新 yyyy-MM-dd HH:mm:ss`）
  - 推送后显示仓库大小变化

### 同步命令

- `ggt sync` - 遍历所有仓库，自动同步
  - 先执行 `git fetch --all --prune`
  - 比较本地、远程、共同祖先的 commit hash
  - 根据情况执行：
    - 本地与远程一致 → 跳过
    - 本地落后于远程（线性更新）→ `git pull --ff-only`
    - 本地领先于远程 → 跳过（提示用户手动推送）
    - 非线性更新 → 提示用户手动干预

### 配置命令

- `ggt repo add <path>` - 添加一个仓库路径到配置文件
- `ggt repo remove <path>` - 从配置文件移除一个仓库路径
- `ggt repo list` - 列出所有已配置的仓库路径
- `ggt repo add-parent <path>` - 添加一个父目录，自动扫描其中的所有 git 仓库路径

配置使用 json 文件存储，默认位置为 `~/.config/go-git-ggt/ggt-config.json`，可通过 viper 调整默认位置。

配置文件中应存储以下内容：添加的仓库父路径列表、添加的独立仓库路径列表、并发数设置等。

### 其他命令

- `ggt owned` - 批量获取所有仓库的所有权，严格遵循当前的 pwsh 命令模仿实现，不要创新和改动。当前的 pwsh 脚本已经十分稳健了。

## 其他要求

- 自文档化，帮助信息应当详尽，所有帮助信息和输出暂且都使用简体中文。
